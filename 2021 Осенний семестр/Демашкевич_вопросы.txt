1.
a) список студентов в группе
В данном случае подойдет список, для легкого измененения данных студента. 
b) список оценок студента
Здесь подойдет кортеж для безопасности данных об оценках и не возможности изменить оценки.
c) данные студента: ФИО, адрес, дата рождения
Для данных студента подойдет словарь, где ключом является ФИО студента или номер студента, остальные данные можно поместить в список.
2.
print ("Напишите количество цифр")
n = int(input())
a = []
print ("Напишите число")
i = 1
while i <= n:
    a.append(int(input()))
    i += 1
b = {}
i = 0
while i < n:
    c = []
    j = 1
    while j <= a[i]:
        c.append(j)
        j += 1
    b[a[i]] = tuple(c)
    i += 1
print (b)